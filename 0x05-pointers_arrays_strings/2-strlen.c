#include "main.h"
/**
* swap_int-reset value to 98
* @a:pointer value
* @b:pointer value
*/
int _strlen(char *s)
{
    int len = 0;

    while(s[len] != '\0')
    {
        len++;
    }
    return len;
}
